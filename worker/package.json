{
  "name": "taskflow-worker",
  "version": "1.0.0",
  "description": "TASKFLOW - Worker independiente para procesamiento asíncrono de solicitudes desde Redis Queue",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "ioredis": "^5.4.1",
    "mongoose": "^8.5.1",
    "socket.io-client": "^4.7.5"
  }
}
